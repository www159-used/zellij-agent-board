[alias]
lint = "clippy -p zellij-agent-board --lib --all-targets -- -D warnings"
e2e = "test -p e2e-scenes"
wasm = "build -p zellij-agent-board --target wasm32-wasip1 --release --features plugin --bin zellij-agent-board"
